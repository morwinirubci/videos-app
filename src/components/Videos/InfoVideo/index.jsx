
const InfoVideo = (props) => {

//    let videoElement = props.map(video => )

    return (

        <div>
            <p>{props.title}</p>
            <p>{props.subtitle}</p>
        </div>

    );

}
export default InfoVideo;